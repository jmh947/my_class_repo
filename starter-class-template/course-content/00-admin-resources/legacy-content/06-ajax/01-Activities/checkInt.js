


function check(num) {
    console.log(Number.isInteger(num));
}

function check(num) {
    console.log(parseInt(num) == num);
}

function check(num) {
    console.log(Math.floor(num) == num);
}

check("7");